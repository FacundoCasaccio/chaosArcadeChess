# Godot .NET Rules
- Prefer modifying existing systems over creating parallel implementations.
- UI changes must be done in scene + controller together and verified in editor.
- Keep logs readable: cap list sizes; avoid per-frame allocations in UI.